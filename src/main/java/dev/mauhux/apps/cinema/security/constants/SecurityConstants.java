package dev.mauhux.apps.cinema.security.constants;

public class SecurityConstants {

    // Token expiration time in milliseconds (15 minutes)
    public final static Long EXPIRE = (long) 900_000;

    // Information about the token issuer (who generates the token)
    public final static String ISSUER_INFO = "Mauhux";
}
